{
  "name": "react-editest",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://shockry.github.io/editest",
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "webpack --config webpack.dev.config.js && react-scripts start",
    "build": "webpack --config webpack.prod.config.js && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
